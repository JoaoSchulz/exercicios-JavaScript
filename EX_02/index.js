var Base = Number(window.prompt('Qual é a Base do retangulo?'));
var Altura = Number(window.prompt('Qual é a Altura do retangulo?'));

var Area = Base * Altura;

document.write(`A area do retangulo é ${Area}`);
